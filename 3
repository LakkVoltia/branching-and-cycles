#include <iostream>

using namespace std;

int main() {
    int year;
        
            // Ввод года
                cin >> year;
                    
                        // Проверка на високосность
                            if ((year % 400 == 0) || (year % 4 == 0 && year % 100 != 0)) {
                                    cout << "YES" << endl;
                                        } else {
                                                cout << "NO" << endl;
                                                    }
                                                        
                                                            return 0;
                                                            }
